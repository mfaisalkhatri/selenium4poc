# To execute this docker-compose yml file use `docker-compose -f docker-compose-v3-juiceshop.yml up -d`
# Add the `-d` flag at the end for detached execution
# To stop the execution, hit Ctrl+C, and then `docker-compose -f docker-compose-v3-juiceshop.yml down`
version: "3"
services:
  juice-shop:
    image: bkimminich/juice-shop
    ports:
      - 3000:3000